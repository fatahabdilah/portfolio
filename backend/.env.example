# =================================================================
# | CORE ENVIRONMENT VARIABLES (TEMPLATE FOR DEVELOPERS)          |
# =================================================================

# -------------------------------------------------
# 1. APPLICATION & SERVER SETTINGS
# -------------------------------------------------
# The port where the Express server will listen.
PORT=5000
# The URL of your frontend application (e.g., for CORS).
FRONTEND_URL=http://localhost:5173
# Node Environment: Set to 'development' locally.
NODE_ENV=development

# -------------------------------------------------
# 2. DATABASE CONFIGURATION (MongoDB)
# -------------------------------------------------
# Your MongoDB Connection String.
MONGO_URI=mongodb+srv://<USER>:<PASSWORD>@cluster0.leegl5f.mongodb.net/myportfolio_db?retryWrites=true&w=majority&appName=Cluster0

# -------------------------------------------------
# 3. AUTHENTICATION (JWT)
# -------------------------------------------------
# A strong, unique secret key used to sign and verify JWT tokens.
SECRET=<YOUR_UNIQUE_JWT_SECRET>
# A separate secret key specifically used for signing password reset tokens.
RESET_SECRET=<YOUR_UNIQUE_PASSWORD_RESET_SECRET>

# -------------------------------------------------
# 4. ADMIN USER & SEEDING SCRIPT
# -------------------------------------------------
# Credentials used by the 'seed' script.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=strongpassword 
ADMIN_NAME=Admin User

# -------------------------------------------------
# 5. FILE STORAGE (Cloudinary)
# -------------------------------------------------
CLOUD_NAME=<YOUR_CLOUD_NAME>
CLOUDINARY_API_KEY=<YOUR_API_KEY>
CLOUDINARY_API_SECRET=<YOUR_API_SECRET>

# -------------------------------------------------
# 6. EMAIL SERVICE (Nodemailer for Password Reset)
# -------------------------------------------------
EMAIL_SERVICE_HOST=smtp.example.com
EMAIL_SERVICE_PORT=587
EMAIL_USER=sender@example.com
EMAIL_PASS=<YOUR_SMTP_PASSWORD>